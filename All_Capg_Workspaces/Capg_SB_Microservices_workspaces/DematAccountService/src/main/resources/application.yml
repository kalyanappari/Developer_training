eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8762/eureka
  instance:
    hostname: localhost
    
server:
  port: 9093
  
spring:
  application:
    name: DematAccountService